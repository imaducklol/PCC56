#include <cmath>
#include <functional>
#include <iostream>
#include <string>
#include <vector>
#include <random>
#include <algorithm>
using namespace std;

int checkBoard (string &board, string check) {
    
}

int main () {
    // Setup
    string board[3][3];
    for (int x = 0; x < 3; x++) {
        for (int y = 0; y < 3; y++) {
            board[x][y] = x * 3 + y + 1;
        }
    }

    // Play
    bool running = true;
    while (running) {
        
    }

}