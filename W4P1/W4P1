#include <ctime>
#include <iostream>
#include <vector>
#include <random>
using namespace std;

int main() {
    mt19937 generator(time(NULL));
    uniform_int_distribution<int> ztoh(0, 100);
    int numbersToSort[20];
    int numbersSorted[20];

    for (int i = 0; i < 20; i++) {
        numbersToSort[i] = ztoh(generator);
    }

    for (int i = 0; i < 20; i++) {
        if (numbersToSort[i] == numbersToSort[i+1] || numbersToSort[i] < numbersToSort[i+1]) {
            numbersSorted[i] = numbersToSort[i];
            numbersSorted[i+1] = numbersToSort[i+1];
        } else if (numbersToSort )
    }

}